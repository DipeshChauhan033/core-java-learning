interface Uname{
    void username(uname);
}

interface Pass{
    void password(pass);
}

class Another{
    System.out.println("Enter Facebook to login with Facebook\nEnter Gmail to login with Gmail:");
}